package ua.deliveryservice.domain;

import static org.mockito.Mockito.*;
import static org.junit.Assert.*;

import org.junit.Test;

public class DiscountPizzaTest {

	@Test
	public void testPriceWithDiscount() {
		/*
		DiscountPizza pizza = new DiscountPizza(1, "", null, 20., 0.3);
		assertEquals((Double)pizza.getPrice(), (Double)14.);
		*/
	}
}
